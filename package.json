{
  "name": "lulu",
  "private": true,
  "version": "1.0.0",
  "workspaces": [ "libs/*", "app" ],
  "scripts": {
    "postinstall": "git submodule update --remote --merge",
    "build": "nx run-many --target=build",
    "test": "nx run-many --target=test"
  },
  "devDependencies": {
    "nx": "15.8.7"
  }
}
